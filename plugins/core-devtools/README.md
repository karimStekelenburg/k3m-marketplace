# core-devtools

A starter plugin for the k3m-marketplace marketplace.

## Structure

- `commands/` — Slash commands
- `skills/` — Skills (auto-triggered prompt templates)
- `.claude-plugin/plugin.json` — Plugin metadata
